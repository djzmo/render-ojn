set(VCPKG_TARGET_ARCHITECTURE x86)
set(VCPKG_CRT_LINKAGE static)
set(VCPKG_LIBRARY_LINKAGE static)
# The x86 comparison build must use the same legacy toolset as x64.
set(VCPKG_PLATFORM_TOOLSET v142)
